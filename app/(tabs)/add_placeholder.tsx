import { View } from 'react-native';

/**
 * This component is a placeholder for the "Add Entry" tab button.
 * The tab press is intercepted in _layout.tsx to open the modal instead.
 * This file is required to silence the "No route named..." warning.
 */
export default function AddPlaceholder() {
  return <View />;
}
